//FOR OF: LISTAS Y ARRAYS
let arreglo = 'hola';

for (let valor of arreglo) {
  console.log(valor);
}

//FOR IN: OBJECTS
let objeto = {
  nombre: 'william',
  edad: 21
}

for (let elemento in objeto) {
  console.log(objeto[elemento]);
}

let mensaje = edad >= 18 ? 'si lo es' : 'no lo es'; //ternario - forma abreviada del if.

